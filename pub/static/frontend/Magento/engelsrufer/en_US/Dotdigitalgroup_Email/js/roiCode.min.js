define(['jquery','dmmpt'],function($){'use strict';function init(items,total){var fLen=items.length,i=0;for(i;i<fLen;i++){window._dmTrack('product',items[i]);}
window._dmTrack('CheckOutAmount',total);window._dmCallHandler();}
return function(roiCode){$(document).ready(function(){init(roiCode.items,roiCode.total);});};});