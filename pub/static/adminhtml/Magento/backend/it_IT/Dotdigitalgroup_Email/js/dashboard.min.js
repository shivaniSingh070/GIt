define(['jquery','domReady!'],function($){'use strict';function bind(id,link){$('#'+id).click(function(){window.location.assign(link);});}
return function(dashboard){bind('contact_sync',dashboard.contactLink);bind('importer_sync',dashboard.importerLink);};});